describe('Loading Functionality', () => {});

describe('Managing Actions', () => {
  describe('Adding Actions', () => {
    test('Add Basic Action', () => {
      expect(false).toBeTruthy();
    });

    test('Add Legendary Action', () => {
      expect(false).toBeTruthy();
    });

    test('Add Lair Action', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('Removing Actions', () => {
    test('Remove Basic Action', () => {
      expect(false).toBeTruthy();
    });

    test('Remove Legendary Action', () => {
      expect(false).toBeTruthy();
    });

    test('Remove Lair Action', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('Modifying Actions', () => {
    test('Modifying Basic Action', () => {
      expect(false).toBeTruthy();
    });

    test('Modifying Legendary Action', () => {
      expect(false).toBeTruthy();
    });

    test('Modifying Lair Action', () => {
      expect(false).toBeTruthy();
    });
  });
});
